sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
a <- space_discovery(sampleI45(df1),1024)
b <- space_discovery(df1c,1024)
c <- space_discovery(sampleI45(df2),1024)
d <- space_discovery(df2c,1024)
rate_plot(a,b,c,d,"expansion rate - 1024 systems")
rate_plot(a,b,c,d,"expansion rate - 1024 systems (45 from I)")
df1_just8 <- subset(df1,iteration > 9)
table(df1_just8$iteration)
df1_just8 <- subset(df1,iteration < 9)
table(df1_just8$iteration)
df2_just8 <- subset(df2,iteration < 9)
df1i_just8 <- subset(df1_just8,condition=="I")
table(df1_just8$iteration)
df1i_just8$condition
df1_just8$condition
df1i_just8 <- subset(df1_just8,condition=="I")
table(df1i_just8$iteration)
df1c_just8 <- subset(df1_just8,condition=="C")
df1i_just8 <- subset(df1_just8,condition=="I")
df1c_just8 <- subset(df1_just8,condition=="C")
df2i_just8 <- subset(df2_just8,condition=="I")
df2c_just8 <- subset(df2_just8,condition=="C")
length(unique(df1_just8$system1024))/nrow(df1_just8)
length(unique(df1_just8$system1024))
nrow(df1_just8)
length(unique(df1i_just8$system1024))
nrow(df1i_just8)
length(unique(df1i_just8$system1024))/nrow(df1i_just8)  # 197 / 435 =
length(unique(df1c_just8$system1024))
nrow(df1c_just8)
length(unique(df1c_just8$system1024))/nrow(df1c_just8)  #  90 / 198 =
length(unique(df1_just8$system512))
nrow(df1_just8)
length(unique(df1_just8$system512))/nrow(df1_just8)    # 188 / 633 =
length(unique(df1i_just8$system512))
nrow(df1i_just8)
length(unique(df1c_just8$system512))
nrow(df1c_just8)
length(unique(df1c_just8$system512))/nrow(df1c_just8)   #  75 / 198 =
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
length(a)
a
head(sampleI45(df1))
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
sampleI45 <- function(df) { # feed it df1 or df2
I_trajectories <- unique(subset(df,condition=="I")$trajectory)  # get all 90 chains in I
I_trajectories_sample <- sample(I_trajectories,45) # randomly choose 45 of them
dfI45 <- df[df$trajectory %in% I_trajectories_sample,]  # 214 or so entries
return(dfI45)
}
a <- space_discovery(sampleI45(df1),512)
b <- space_discovery(df1c,512)
c <- space_discovery(sampleI45(df2),512)
d <- space_discovery(df2c,512)
rate_plot(a,b,c,d,"expansion rate - 512 systems (45 from I)")
# important note:
# each chain has a variable number of tries (iterations) in it
# so matching chain number doesn't actually match the number of tries
# compare:
nrow(sampleI45(df1))
nrow(df1c)
